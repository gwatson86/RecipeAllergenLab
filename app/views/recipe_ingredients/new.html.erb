<h1>New RecipeIngredient Form</h1>

<%= form_with model: @recipe_ingredient do |f| %>
    <label>Name: </label>
    <%= f.text_field :name %><br>
    <label>User: </label>
    <%= f.collection_select :user_id, User.all, :id, :name %><br>
    <%= f.submit %>
<% end %>